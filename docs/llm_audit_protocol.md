# LLM Audit Protocol

The LLM audit is an auxiliary quality-control process. It is not treated as human inter-annotator agreement.

The audit compares ChatGPT, Gemini, and DeepSeek outputs with gold labels on a stratified subset.
